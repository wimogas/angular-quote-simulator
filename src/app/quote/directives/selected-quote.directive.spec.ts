import { SelectedQuoteDirective } from './selected-quote.directive';

describe('SelectedQuoteDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectedQuoteDirective();
    expect(directive).toBeTruthy();
  });
});
